/**
 * the schema definitions of form
 */
class Schema {
    constructor (conf) {
        this.conf = this.build(conf)
    }

    build (conf) { // TODO: 校验，格式转换等功能
        return conf
    }
}

export default Schema
